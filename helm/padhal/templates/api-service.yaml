apiVersion: v1
kind: Service
metadata:
  name: {{ include "padhal.apiName" . }}
  labels:
    {{- include "padhal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  selector:
    {{- include "padhal.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
